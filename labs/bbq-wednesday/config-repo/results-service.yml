# ---------------------------------------------------------------------------
# results-service — centrally managed knobs for the live tally aggregator.
# ---------------------------------------------------------------------------
# The UI polling interval is defined in static/js/results.js (2000 ms).

# Demonstrates environment-specific values without repackaging:
# a "prod" profile could ship a different banner via results-service-prod.yml.
info:
  tally: "BBQ Wednesday — live from Kansas City"
