spring:
  application:
    name: survey-ui

server:
  port: 8091

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

logging:
  level:
    com.bbqwednesday: DEBUG
